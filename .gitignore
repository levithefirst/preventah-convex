node_modules
dist

# Vite's convention, and the reason .env itself is tracked: .env holds the
# public deployment URL the build needs, .env.local holds anything secret.
.env.local
.env.*.local

# tsc -b writes these next to the configs; they are build state, not source.
*.tsbuildinfo
