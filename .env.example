# Written by `npx convex dev`. Points the Vite app at your deployment.
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=

# Set on the DEPLOYMENT, not in this file:
#   npx convex env set FIRECRAWL_API_KEY ...
#   npx convex env set AGENTMAIL_API_KEY ...
#   npx convex env set AGENTMAIL_INBOX_ID ...    # optional; otherwise the first inbox is used
#
# Convex actions read these from process.env at run time. Never commit values.
